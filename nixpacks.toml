[phases.build]
cmds = ["npm run build"]
cacheDirectories = []

[start]
cmd = "npx serve -s dist -l 3000"

[staticAssets]
Caddyfile = '''
:{$PORT:3000} {
    root * ../app/dist
    encode gzip
    file_server
    try_files {path} /index.html
}
'''